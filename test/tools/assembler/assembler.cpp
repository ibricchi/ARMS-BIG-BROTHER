#include <iostream>
#include <vector>
#include <unordered_map>

#include <scanner.h>

using namespace std;

int main(){
    unordered_map<string, uint32_t> labels{};

    string line;
    getline(cin, line);
    for(; !cin.fail(); getline(cin, line)){
        for(auto it = line.begin(); it != line.end(); it++){
            
        }
    }

    vector<uint32_t> mem{};
}

